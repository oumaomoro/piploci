# Piploci — Streamlit Cloud Dependencies
# Only packages required by dashboard.py (the REST API client dashboard).
# Backend server dependencies are in requirements-backend.txt.
streamlit>=1.32.0
pandas>=2.2.0
requests>=2.31.0
python-dotenv>=1.0.0
pytz>=2024.1
