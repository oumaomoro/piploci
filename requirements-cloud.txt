# Streamlit Cloud Dashboard Dependencies (dashboard.py only)
# The full backend (server.py, bot_engine.py) runs separately on your own server.
streamlit>=1.32.0
pandas>=2.2.0
requests>=2.31.0
python-dotenv>=1.0.0
